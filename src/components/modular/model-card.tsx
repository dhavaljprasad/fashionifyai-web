"use client";

type ModelCardDataType = {
  model_name: string;
  gender: string;
  image_url: string;
  model_id: string;
};

export const ModelCard = ({ data }: { data: ModelCardDataType }) => {
  return <div></div>;
};
